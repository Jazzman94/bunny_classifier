# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Raw image dataset (repo root only — src/bunny_classifier/data is code).
# `/data/*` + negation so the split manifest IS tracked while images are not.
/data/*
!/data/manifest.csv

# Generated reports (EDA artifacts, near-dup review sheets)
/reports/

# Virtual environments
.venv

# Tool caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Experiment tracking
mlruns/
runs/

# Claude
.claude
context.txt

# Local planning/working docs (kept out of the repo; copy between
# machines manually alongside the raw images)
ROADMAP.md
HANDOFF.md
